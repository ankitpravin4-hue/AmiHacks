# Vulnerable demo target (Phase 1)
HOST=127.0.0.1
PORT=8000
DATABASE_URL=sqlite:///./data/shop.db

# Scanner allow-list (Phase 2+) — never scan hosts not listed here
ALLOWLIST=http://127.0.0.1:8000
SAFE_MODE=true
