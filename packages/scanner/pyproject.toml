[project]
name = "sentinel-scanner"
version = "0.2.0"
description = "SentinelAPI scanner core and service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
